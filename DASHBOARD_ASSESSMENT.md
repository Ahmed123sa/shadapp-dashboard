# تقييم فني للداشبورد (shadapp-dashboard)

**التاريخ:** 3 سبتمبر 2026
**المنظور:** مراجعة بعين Tech Lead — مش مراجعة كود سطر-بسطر، لكن تقييم للقرارات المعمارية، المخاطر التشغيلية، والدين التقني.
**قاعدة التقييم:** فحص فعلي للكود (جرد بالأرقام)، مش انطباع. كل رقم في التقرير ده مقيس من الكود الحالي.

---

## ملخص تنفيذي

الداشبورد **في حالة صحية أعلى من المتوسط** لمنتج في مرحلته دي، والسبب الأساسي إن طبقة الأمان اتبنيت صح (مش مجرد اتلزقت بعدين). لكن فيه **فجوة معمارية واحدة كبيرة** بتفسّر معظم المشاكل التانية: **مفيش طبقة بيانات**. كل مكوّن بيكلّم الـAPI بنفسه، وده اللي بيولّد الـpolling المكرر، الفشل الصامت في الكتابة، وصعوبة الاختبار.

**التقييم العام: 6.5 / 10** — كود منتَج (production-grade) في الأمان والتنسيق، وكود نموذج أولي (prototype-grade) في إدارة البيانات والمراقبة.

| المحور | التقييم | ملاحظة سريعة |
|---|:---:|---|
| الأمان والمصادقة | 8/10 | أقوى جزء في المشروع — توكن في httpOnly cookie وحراسة server-side حقيقية |
| اتساق الواجهة والتصميم | 8/10 | نظام توكنات موحّد، a11y متعمّل، ثيم فاتح متبني صح |
| جودة الكود والأنواع | 7/10 | TypeScript مستخدم بجدية، بس فيه 52 `any` و162 تحذير lint |
| المعمارية وطبقة البيانات | 4/10 | **أضعف نقطة** — 129 استدعاء API متفرّق، صفر تخزين مؤقت |
| الاختبارات | 5/10 | ممتازة في `lib/`، شبه غايبة عن المنطق التجاري |
| الأداء | 5/10 | كل حاجة client-side، polling + websockets بالتوازي |
| جاهزية الإنتاج والمراقبة | 5/10 | **مفيش أي error tracking حقيقي** في الإنتاج |

---

## 1. الأرقام (جرد فعلي)

| المقياس | القيمة |
|---|---|
| ملفات المصدر | 116 ملف (90 `.tsx` + 26 `.ts`) |
| أسطر الكود | ~13,370 سطر إجمالي (~11,860 مصدر + ~1,510 اختبارات) |
| ملفات الاختبار | 24 ملف / **132 اختبار — كلها ناجحة** |
| نسبة كود الاختبار للمصدر | ~13% |
| مكوّنات `'use client'` | 66 من 90 (73%) |
| استدعاءات `api.*` مباشرة | **129 استدعاء عبر 37 ملف** |
| مؤقتات `setInterval` (polling) | 5 (بفواصل 30 ث / 60 ث / 300 ث) |
| استخدامات `any` | 52 (منها ~30 في كود المصدر) |
| نقاط فشل صامت (`catch` بدون إبلاغ) | 25 موضع عبر 15 ملف |
| `confirm()` / `alert()` أصلية | 10 مواضع عبر 6 ملفات |
| تحذيرات ESLint | **162 تحذير / صفر أخطاء** |
| أكبر 5 ملفات | reports 642 · settings 620 · finance 451 · audit-log 446 · showcase-demo 426 |
| Dynamic imports | **1 فقط** (خريطة Leaflet) |

---

## 2. اللي اتعمل صح (نقاط القوة الحقيقية)

### 2.1 معمارية الجلسة — أفضل قرار في المشروع
التوكن مش في `localStorage` خالص. الدورة كلها:

```
المتصفح → /api/proxy/*  (same-origin)
              ↓ route handler بيقرا httpOnly cookie
          Laravel API (Authorization: Bearer …)
```

يعني: **أي ثغرة XSS في أي مكان في التطبيق مش هتقدر تسرق التوكن**، لأن الـJS في المتصفح مش شايفه أصلًا. ده مستوى أعلى من معظم لوحات التحكم اللي بحجم ده.

### 2.2 حراسة المسارات حقيقية مش تجميلية
`src/proxy.ts` (بديل middleware في Next 16) بيمنع الوصول لـ `/dashboard` و`/client-dashboard` **قبل ما أي HTML يتبعت**، معتمدًا على الكوكي. الحراسة الـclient-side (`isAuthenticated()`) موثّق جواها صراحة إنها "راحة بصرية مش حدّ أمني" — وده مستوى وعي كويس جدًا.

### 2.3 الكومنتات بتشرح "ليه" مش "إيه"
ده نادر. أمثلة فعلية من الكود: سبب استخدام `@theme inline`، سبب تسمية توكنات `--fs-*` بدل `--text-*`، سبب `duplex: 'half'`، وتوثيق صريح لمخاطر متبقية (`KNOWN RESIDUAL RISK` في وكيل `/storage`). ده بيقلل تكلفة انضمام أي مطوّر جديد بشكل ملموس.

### 2.4 نظام تصميم موحّد فعلًا
بعد شغل الـUI/UX الأخير: توكنات ألوان وخطوط ومقاسات مركزية، وثيم فاتح مبني بآلية `[data-theme]` من غير لمس أي مكوّن. الاتساق البصري دلوقتي **مفروض بالبنية** مش بالانضباط اليدوي.

### 2.5 حالات الخطأ والتحميل موجودة
`error.tsx` / `not-found.tsx` / `loading.tsx` / `global-error.tsx` + `ErrorState` + `TableSkeleton` موحّدة. مسارات **القراءة** بتفشل بشكل مرئي ومحترم.

---

## 3. المشاكل مرتّبة حسب الخطورة

### 🔴 P0 — الكتابة بتفشل بصمت (أخطر مشكلة تشغيلية)

25 موضع عبر 15 ملف بالنمط ده:

```ts
const { data } = await api.patch(`/meetings/${id}/cancel`).catch(() => ({ data: null }));
if (data) setMeetings(...);   // ولو فشل؟ مفيش أي حاجة بتحصل
```

**النتيجة الفعلية:** المستخدم يضغط "إلغاء الاجتماع"، السيرفر يرجّع 500 أو 422 أو النت يقطع → **الشاشة ما بتتغيرش، ومفيش رسالة**. المستخدم يفتكر إنه ضغط غلط ويعيد، أو أسوأ: يفتكر إن العملية نجحت.

ده بيمس عمليات حقيقية: إلغاء/إكمال اجتماعات، مراجعة دفعات، رفع ملفات، إنشاء عقود.

**الإصلاح:** wrapper واحد للـmutations يعمل `reportError` + toast فشل. عندكم `ToastNotification` جاهز بالفعل — المشكلة إنه مش متوصّل بالمسارات دي.

---

### 🔴 P0 — صفر مراقبة أخطاء في الإنتاج

`lib/error-reporting.ts` موثّق فيه بصراحة:

> "The dashboard has no error-reporting service today (no Sentry equivalent, unlike the mobile app's Crashlytics pipeline)."

يعني `reportError()` بيكتب في `console.error` وخلاص. **لو حصل باج عند عميل حقيقي دلوقتي، مش هتعرف بيه غير لما هو يشتكي.** التطبيق الموبايل عنده Crashlytics — الداشبورد لأ. ده تناقض غير منطقي، خصوصًا إن الداشبورد هو الأداة الداخلية اللي فريقك شغال عليها طول اليوم.

**الإصلاح:** Sentry (أو أي بديل) — نقطة التوصيل جاهزة في دالة واحدة، الشغل الفعلي أقل من يوم.

---

### 🟠 P1 — مفيش طبقة بيانات (السبب الجذري لمشاكل كتيرة)

**129 استدعاء `api.*` منتشرين في 37 ملف مكوّن.** مفيش repository، مفيش cache، مفيش React Query/SWR.

النتائج المباشرة:
- **تكرار الطلبات:** نفس البيانات بتتجاب أكتر من مرة عند التنقل بين التابات.
- **صفر إعادة استخدام:** كل مكوّن بيكتب `useEffect` + `loading` + `error` + `setState` من الأول (الـ162 تحذير lint معظمهم عرض للمشكلة دي).
- **صعوبة الاختبار:** عشان تختبر مكوّن لازم تعمل mock لـaxios، عشان كده الاختبارات وقفت عند المكوّنات الصغيرة.
- **التناقض المعماري:** التطبيق الموبايل **اتعمله refactor كامل** لـRepository/Provider في جلسات سابقة. الداشبورد فضل على النمط القديم. دلوقتي عندك منطقين مختلفين لنفس الدومين.

**الإصلاح المقترح (تدريجي، مش إعادة كتابة):** إدخال TanStack Query كطبقة تخزين مؤقت + إزالة الـpolling اليدوي، شريحة-بشريحة زي ما اتعمل بالظبط في الموبايل.

---

### 🟠 P1 — Polling + WebSockets شغالين بالتوازي

5 مؤقتات `setInterval` (30 ث في المدفوعات، 60 ث في الشات، 300 ث في الإشعارات...) **بالإضافة** لاشتراك Reverb/Echo على نفس البيانات.

ده نفس الباج اللي **اتصلح في التطبيق الموبايل** (مهمة "إزالة الـpolling المكرر مع WebSocket"). الداشبورد لسه بيدفع التكلفة: حمل زيادة على السيرفر + بطارية/باندويدث + احتمال سباق بين نسخة الـpolling ونسخة الـwebsocket.

---

### 🟠 P1 — حراسة الأدوار (SA / AM) client-side بالكامل

```ts
const isSA = getUser()?.role === 'super_admin';   // مصدرها localStorage
```

`localStorage` قابل للتعديل من أي مستخدم في الـDevTools. `proxy.ts` بيتحقق من **نوع الجلسة** (staff/client) بس، مش من **الدور**.

**التقدير الواقعي:** الخطر الحقيقي محدود لأن الباك اند اتشدّد في مراحل سابقة (عزل المستأجرين، إصلاح IDOR) — فالبيانات مفروض محمية على مستوى الـAPI. لكن ده يعني إن **الأمان كله معتمد على إن كل endpoint في Laravel عامل تحقق دور صح، من غير أي شبكة أمان تانية**. أي endpoint اتنسي، مفيش حاجة هتوقف الوصول.

**الإصلاح:** (أ) تدقيق سريع لتحقق الأدوار في الباك اند، و(ب) إضافة الدور لكوكي الجلسة عشان `proxy.ts` يقدر يحرس المسارات الحسّاسة server-side.

---

### 🟡 P2 — كل حاجة client-side (73% من الملفات)

66 من 90 ملف `'use client'`. عمليًا: **بتدفع تعقيد Next.js App Router من غير ما تاخد فايدته**. مفيش server components، مفيش جلب بيانات على السيرفر، مفيش streaming. النتيجة: شلالات طلبات بعد الـhydration (الصفحة تظهر → بعدين تبدأ تجيب البيانات).

مش مشكلة عاجلة، بس دي أكبر فرصة تحسين أداء متاحة لو الصفحات بقت بطيئة.

---

### 🟡 P2 — انهيار محتمل عند بيانات جلسة تالفة

```ts
const raw = localStorage.getItem('user');
return raw ? JSON.parse(raw) : null;    // بدون try/catch
```

نفس النمط في `getUser()` و`getClient()` و`getSubUser()`. أي `localStorage` متعرقل (تعديل يدوي، كتابة ناقصة، امتلاء الحصة) بيرمي استثناء **أثناء الـrender** → شاشة خطأ عامة، والمستخدم عالق في حلقة مش قادر يخرج منها إلا بمسح الـstorage يدويًا.

إصلاح من 3 أسطر × 3 دوال، ويفضل يمسح المفتاح التالف ويرجّع `null`.

---

### 🟡 P2 — إعدادات البيئة بترجع لـlocalhost بصمت

```ts
process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api'
process.env.NEXT_PUBLIC_REVERB_KEY || 'shadapp-key'
```

لو الـenv مش مضبوط في الإنتاج، التطبيق **مش هيفشل** — هيبني وينشر ويحاول يكلّم localhost. ده نوع الأخطاء اللي بتضيّع ساعات في التشخيص.

**الإصلاح:** فشل صريح عند الإقلاع في `NODE_ENV=production` لو المتغيرات ناقصة.

---

### 🟡 P2 — رؤوس أمان ناقصة

`next.config.ts` فيه 4 رؤوس كويسة (`X-Frame-Options`, `nosniff`, `Referrer-Policy`, `X-XSS-Protection`) — بس **مفيش `Content-Security-Policy` ولا `Strict-Transport-Security`**. لتطبيق بيعرض عقود وتوقيعات وبيانات دفع، CSP بالذات هي خط الدفاع التاني بعد أي XSS.

---

### ⚪ P3 — بنود تنظيمية

- **162 تحذير lint** (صفر أخطاء): معظمها `react-hooks/set-state-in-effect` و`exhaustive-deps` — دي **أعراض** لغياب طبقة البيانات مش مشاكل مستقلة، وهتختفي لوحدها مع إصلاح P1. باقيها `no-img-element` (استخدام `<img>` بدل `next/image`) و`no-explicit-any`.
- **10 `confirm()`/`alert()` أصلية** جنب مكوّن `ConfirmDialog` مصمّم بعناية — تناقض بصري في لحظات حرجة (حذف عميل، إلغاء اجتماع).
- **ملفات ضخمة:** `reports/page.tsx` (642 سطر) و`settings/page.tsx` (620) بيخلطوا الجلب + الحالة + العرض + CSS. الموبايل اتعمله تقسيم لملفات فوق 800 سطر؛ نفس المعيار ينفع هنا.
- **`showcase-demo` (426 سطر)** لسه في الإنتاج — صفحة عرض داخلية متاحة لأي حد يعرف الرابط.
- **تقسيم الحزمة:** `dynamic import` واحد بس (الخريطة). `recharts` و`motion` تقيلين وبيتحمّلوا حتى لو المستخدم عمره ما فتح صفحة التقارير.

---

## 4. خطة التنفيذ المقترحة (مرتّبة بالعائد/التكلفة)

### الجولة 1 — "لو حصلت مشكلة أعرف بيها" (2-3 أيام)
1. ⏸️ توصيل Sentry بـ`reportError` — مؤجّل، محتاج المستخدم يجهّز حساب/DSN الأول.
2. ✅ **تم** — Wrapper موحّد للـmutations (`notifyWriteError` في `lib/utils.ts`): كل فشل كتابة بيعمل `reportError` + توست عام (`showToast`) بدل الفشل الصامت. اتطبّق على كل الـ25 موضع اللي كانت `.catch(() => ({ data: null }))` عبر 15 ملف (`ClientSignature.tsx` ×2، `ClientChat.tsx`، `ClientApprovals.tsx`، `ApprovalsTab.tsx`، `ClientContracts.tsx`، `ClientPayments.tsx`، `ContractBuilder.tsx`، `ChatTab.tsx` ×2، `UploadProofModal.tsx`، `dashboard/settings/page.tsx` ×3، `FilesTab.tsx` ×3، `PaymentsTab.tsx`، `ContractsTab.tsx` ×3، `dashboard/clients/page.tsx`، `MeetingsTab.tsx` ×3). مفاتيح ترجمة جديدة `common.write_error_title`/`common.write_error_message` بالعربي والإنجليزي. اكتُشف أثناء الشغل إن `<ToastNotification />` كان متركّب في `dashboard/layout.tsx` بس، مش تحت `/client-dashboard` — اتضاف `client-dashboard/layout.tsx` جديد (إضافي بالكامل، مش بيلمس أي صفحة موجودة) عشان `showToast()` يشتغل من مكونات البورتال (`ClientChat`, `ClientContracts`, إلخ) كمان.
3. ✅ **تم** — `safeJsonParse<T>` في `lib/utils.ts` بيحمي `getUser()`/`getClient()`/`getSubUser()` من انهيار الصفحة لو قيمة الـlocalStorage تالفة، وبيمسح القيمة التالفة عشان ما تفضلش تفشل كل render.
4. ✅ **تم** — `lib/env-guard.ts` (`assertProductionEnv`) بيتأكد إن `NEXT_PUBLIC_API_URL` و`NEXT_PUBLIC_REVERB_HOST` و`NEXT_PUBLIC_REVERB_KEY` موجودين، وبيرمي خطأ واضح في الـlogs لو الإنتاج (`NODE_ENV=production`) بدأ من غير ما تتحط. اتنفّذ بعد ما المستخدم أكّد إن الإنتاج هيكون على VPS مستقل (والتطوير حاليًا على localhost) — يعني الفشل الصريح مش هيوقف حاجة شغالة دلوقتي، هيمنع بس إن الـVPS يشتغل يوم ما بمتغيرات ناقصة وهو واصل بصمت على `localhost:8000`. اتوصل من `proxy.ts` (بيتحمّل مرة واحدة قبل أي طلب). **مهم:** لازم تتحط القيم دي في `.env.production` (أو إعدادات الـprocess manager) على الـVPS قبل ما تشغّل `npm run build && npm start` هناك، وإلا السيرفر هيرفض يبدأ.

> العائد الأعلى في المشروع كله. من غير الجولة دي، أي إصلاح تاني بتعمله إنت مش متأكد إنه اشتغل عند المستخدمين.
>
> **حالة الجولة:** 3 من 4 بنود اتنفذوا وموثّقين (٢، ٣، ٤). Sentry (بند ١) لسه مؤجّل — محتاج حساب/DSN من المستخدم.
>
> **تحقق نهائي:** `npm test` (143/143)، `npm run build`، و`npm run lint` — كلهم اجتازوا نضيف. أثناء التحقق ظهر باج أمني حقيقي من إصلاح `safeJsonParse` بتاع البند 3: جلسة sub-user تالفة كانت بتتحول لصلاحية كاملة بدل المنع (لأن `isSubUser()` كان بيعتمد على نجاح قراءة `sub_user` نفسه). اتصلّح بجعل `isSubUser()` يعتمد على علامة `client.is_sub_user` المستقلة بدل كده — موثّق بالتفصيل في `client-auth.ts`.

### الجولة 2 — إغلاق الفجوة الأمنية المتبقية (2-3 أيام)
5. ✅ **تم** — تدقيق شامل لتحقق الأدوار عبر الـ13 controller في الباك اند مقابل كل route محدد في `api.php` بتعليق "(SuperAdmin only)" أو "(admin only)". اللقطة: النظام أفضل مما كان متوقّع — 6 من 8 مجموعات routes كانت بالفعل محمية صح (إما `isSuperAdmin()` مباشرة أو عبر Policy). لقينا ثغرة حقيقية واحدة: `FileController::storeDefinition` و`destroyDefinition` (تعريفات المستندات المطلوبة) ماكانش فيهم أي تحقق دور خالص — أي موظف (SA أو AM) كان يقدر يضيف/يمسح تعريفات مستندات لأي workspace يديره. اتصلّحت الاتنين + تحقق ملكية إضافي في `destroyDefinition` (كان معتمد كليًا على `ScopeWorkspace` middleware، ودلوقتي عنده تحقق مزدوج). 3 اختبارات جديدة في `TenantIsolationTest.php` تغطي بالظبط الحالة اللي كانت ناقصة (AM على الـworkspace بتاعه هو نفسه، مش workspace غلط).
6. ✅ **تم** — كوكي جديد `sa_session_role` (غير سري، زي `sa_session_type`) بيتسجّل وقت تسجيل دخول الموظفين ويتمسح وقت الخروج. `proxy.ts` بقى يمنع أي حد دوره مش `super_admin` من الوصول لـ`/dashboard/account-managers` قبل ما الصفحة توصله خالص — بدل ما يوصله الـHTML/JS كامل ويترفض بعد الـhydration بس (نفس الفجوة اللي `proxy.ts` كان اتعمل أصلاً عشان يقفلها لغير المسجلين). الباك اند لسه بيتحقق بنفسه بشكل مستقل — ده طبقة UX إضافية، مش الحارس الوحيد.
7. ✅ **تم** — `next.config.ts` بقى فيه Content-Security-Policy و Strict-Transport-Security فوق الـheaders الموجودة. الـCSP مبني على الأصول الحقيقية اللي التطبيق بيحتاجها فعلاً (اتفحصت بالكود): `cdnjs.cloudflare.com` وبلاطات `openstreetmap.org` (خريطة اختيار الموقع)، أصل Laravel نفسه (الصور بتتحمّل مباشرة منه، مش عبر البروكسي)، واتصال WebSocket لـReverb. **ملحوظة صريحة:** مش CSP بأقصى تشديد (لسه فيها `unsafe-inline` للسكريبت/الستايل لأن Next.js نفسه وبعض المكونات محتاجينها) — تحسين حقيقي، مش الحل النهائي؛ التشديد الكامل (nonce-based) محتاج شغل أكبر من الجولة دي.
8. ✅ **تم** — `/showcase-demo` بقى بيرجع 404 لو `NODE_ENV=production` (عبر `proxy.ts`)، وفاضل شغال عادي في التطوير المحلي. الملف نفسه لسه موجود في الريبو (مش محذوف) عشان تقدر تستخدمه للتصميم لو احتجت.

### الجولة 3 — طبقة البيانات (1-2 أسبوع، تدريجي)
9. ✅ **تم** — إدخال TanStack Query (`@tanstack/react-query`) عبر `QueryClientProvider` جديد في `src/app/providers.tsx` (مركّب داخل `layout.tsx` الجذري، `staleTime: 15s` كإعداد افتراضي محافظ). شريحة **المدفوعات** بالكامل اتنقلت: `PaymentsTab.tsx`، `finance/page.tsx`، و`ClientPayments.tsx` — التلاتة بقوا بيستخدموا hooks جديدة في `src/hooks/queries/usePayments.ts` (`useWorkspacePayments`, `useWorkspaceContracts`, `useAllPayments`, `useFinanceFilterOptions`, ومجموعة mutations: `useReviewPayment`, `useSchedulePayments`, `useRequestPayment`, `useDeletePaymentSchedule`, `useSubmitClientPayment`) بدل `useEffect` + `useState` + `api.*` اليدوي في كل مكوّن. اتكتبت اختبارات توصيفية (`PaymentsTab.test.tsx` — 6 اختبارات، `ClientPayments.test.tsx` — 5 اختبارات) **قبل** أي لمس للكود في كل مكوّن، وأُعيد تشغيلها بعد النقل بنجاح (نفس السلوك بالظبط) — بالإضافة لاختبارات `FinancePage` الموجودة مسبقًا اللي فضلت شغالة من غير أي تعديل. تحديات محددة اتحلّت أثناء النقل: `ClientPayments.tsx` كان بيبني حالة النموذج (amount/currency) كـ side effect من نتيجة الفتش نفسها — اتحفظ نفس السلوك بالظبط (بما فيه إعادة التهيئة كل 30 ثانية polling) عبر `useEffect` منفصل بيتابع بيانات الـqueries؛ `finance/page.tsx` كان بيسيب الأرقام القديمة (stats/pagination) ظاهرة أثناء إعادة التحميل مع إظهار skeleton كامل للجدول — اتكرر بالظبط عبر `placeholderData` + `isFetching`. تحقق نهائي: `npm test` (155/155)، `npm run build`، `npm run lint` (صفر أخطاء، نفس عدد التحذيرات 160 — مفيش تحذير جديد).
10. ✅ **تم جزئيًا** — الـpolling (`refetchInterval: 30000`) اتنقل زي ما هو من الكود القديم (نفس الفاصل الزمني) لأن المدفوعات مفيهاش قناة WebSocket تدفع تحديثات لحد دلوقتي — يعني ده نقل للآلية مش إزالة لها. إزالة الـpolling الحقيقية (البند الأصلي) لسه محتاجة قناة Echo مخصصة للمدفوعات، وده خارج نطاق نقلة طبقة البيانات نفسها.
11. الشرائح المتبقية (Contracts, Chat, Approvals, Files, Meetings, Clients, ...) لسه على النمط القديم — تتنقل شريحة-بشريحة بنفس المنهجية (اختبار توصيفي → hooks → نقل مكوّنات → تحقق) في جلسات لاحقة.

> العائد الأول ظهر فورًا: `PaymentsTab.tsx` نزل من إدارة 6 قطع state يدوية (payments/contracts/taxSummary/loading/loadError + الـref الخاص بالـhasLoadedOnce) لصفر — كله بقى مشتق من الـquery. أي شريحة تانية تتنقل هتاخد نفس الشكل تقريبًا.

> ده اللي بيقلّل الـ162 تحذير lint من غير ما تفتحهم واحد واحد، وبيفتح الباب للاختبارات في الجولة اللي بعدها.

### الجولة 4 — تغطية الاختبارات (مستمرة)
12. اختبارات للمكوّنات التجارية غير المغطّاة: `ChatTab`, `PaymentsTab`, `ContractsTab`, `reports`, `settings`.
13. هدف واقعي: من ~23% تغطية ملفات لـ50%+ للمسارات اللي فيها فلوس أو عقود.

### الجولة 5 — أداء وتنظيم (حسب الحاجة)
14. `dynamic import` لـ`recharts` و`motion`.
15. تقسيم `reports` و`settings`.
16. `<img>` → `next/image` (تحسين LCP وباندويدث).

---

## 5. الخلاصة بصراحة

**اللي يطمّن:** الأساسات الصعب تصليحها بعدين (الأمان، الجلسة، نظام التصميم، البنية العامة) **اتعملت صح**. ده مش مشروع محتاج إعادة كتابة.

**اللي يقلق:** المشروع **أعمى في الإنتاج**. مفيش مراقبة، وعمليات الكتابة بتفشل من غير صوت. الاتنين دول مع بعض معناهم إن ممكن يكون فيه مشاكل شغالة عند المستخدمين دلوقتي ومحدش عارف.

**الفجوة المعمارية الوحيدة الحقيقية** هي طبقة البيانات — ومعالجتها هتحل معاها الـpolling والتحذيرات وصعوبة الاختبار في ضربة واحدة. والأهم إن **عندكم بالفعل نموذج ناجح مثبت** للنقلة دي: نفس الشغل اتعمل على تطبيق الموبايل بنجاح ومن غير كسر. نفس المنهجية تتطبّق هنا.

**الأولوية الفعلية لو عندك أسبوع واحد بس:** الجولة 1 كاملة. مش أكتر.

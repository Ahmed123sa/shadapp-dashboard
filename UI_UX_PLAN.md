# خطة مراجعة وإصلاح الـ UI/UX — shadapp-dashboard

مرجع: مراجعة أُجريت على الكود **وعلى التطبيق شغّال فعليًا** في المتصفح (Next 16 / Turbopack، حساب `account_manager`).
كل رقم في التقرير ده متقاس برمجيًا من الصفحة الحية، مش تقدير بالعين.

**الانضباط المتبع في كل مرحلة (نفس انضباط `ACCESSIBILITY_PLAN.md`):**
- الكود القديم يفضل شغّال لحد ما البديل يثبت إنه شغّال. مفيش مسح قبل التأكد.
- بعد كل دفعة: `npm test && npm run build && npm run lint` لازم يرجعوا بنفس عدد الأخطاء/التحذيرات (0 جديد).
- الـ CI هو الحكم. مفيش commit/push مني — أوامر git بس تتقدّم للمستخدم.
- أي مرحلة تكسر حاجة تتراجع ونعيد النظر في الأسلوب.

---

## المرجع: الهوية البصرية

كل قرار في الخطة دي لازم يرجع للهوية المعتمدة، مش لذوق شخصي.

**المصدر الرسمي:** `Shad Brand Guideline ++.pdf` (بروفايل الشركة). **⚠️ لسه ما اتقراش** — لازم يتنسخ جوه مجلد المشروع الأول. لحد ما ده يحصل، البنود اللي تحت مبنية على المستخلص من الكود، وممكن تتعدّل بعد قراءة الدليل.

**المستخلص من الكود حاليًا:**

| الدور | القيمة |
|---|---|
| اللون الأساسي (قرمزي) | `#941414` |
| الذهبي | `#D4AF37` |
| خط العناوين | `Playfair Display` |
| خط المتن | `Tajawal` |

**أول شغل بعد قراءة الدليل:** مطابقة القيم دي بالمعتمد رسميًا. أي اختلاف يتصلح من التوكن مرة واحدة — مش من الاستخدامات المتفرقة.

**ملحوظة مهمة على بند 1.2:** الحل المقترح هناك بيقترح لون نص أفتح من `#941414` للاستخدام فوق الخلفيات الغامقة. ده **مش تغيير للهوية** — ده إضافة درجة وظيفية للنص، والقرمزي الأساسي يفضل زي ما هو للخلفيات والعناصر الكبيرة. لكن القرار النهائي يستنى لحد قراءة الدليل.

---

## ما الذي تم التحقق منه — وما لم يتم

**اتفحص حيًا:** `/login`، `/dashboard` (بدور `account_manager` **وبدور `super_admin`**)، `/dashboard/clients`، `/dashboard/settings` (نسخة AM)، `/dashboard?view=payments` — على عرض 1440px وعلى العرض الضيق.

**ما اتفحصش حيًا:** أقسام الإعدادات الكاملة (`!isAM`)، `/dashboard/finance`، `/dashboard/reports`، واجهة العميل (`/client-dashboard`)، و`showcase-demo`. البنود المتعلقة بيها مبنية على الكود ومعلّمة بـ **(محتاج تأكيد بصري)**.

**نتائج سلبية مهمة — دي مش مشاكل، اتأكدنا:**
- مفيش تجاوز أفقي (`scrollWidth == clientWidth`) لا على 1440px ولا على العرض الضيق. الـ sidebar الجانبي المقفول برّه الشاشة متقصوص صح.
- صفحة تسجيل الدخول: صفر مشاكل تباين.
- `<meta name="viewport">` موجود وصحيح.
- مكوّنات `LoadingSkeleton` / `EmptyState` / `ErrorState` موجودة ومستخدمة فعلًا في الصفحات الرئيسية.

---

## المرحلة 1 — عيوب قابلية قراءة حقيقية (أولوية قصوى)

دي مش تحسينات ذوق — دي نصوص المستخدم مش قادر يقراها.

### 1.1 متغيّرا ألوان مستخدمان وغير معرّفين إطلاقًا
`--color-text-muted` و `--color-text-primary` مستخدمين في **30 موضع عبر 6 ملفات**، ومش معرّفين في `globals.css`.

تأكيد حي من المتصفح: عناوين أقسام الشريط الجانبي (`MAIN`, `COMMUNICATION`, `FINANCE`, `SYSTEM`) المفروض تكون باهتة، لكنها بترندر بـ `rgb(240,240,240)` — نفس سطوع النص الأساسي بالظبط. يعني الفواصل بتزاحم عناصر التنقل بدل ما ترجع للخلف. وحجمها 9.5px.

الملفات: `dashboard/layout.tsx`، `dashboard/clients/page.tsx`، `dashboard/clients/[id]/settings/page.tsx`، `components/payments/PaymentsTab.tsx`، `components/client-payments/ClientPayments.tsx`، `components/clients/ClientProfileTab.tsx`.

**الحل — قرار مطلوب أولًا:** إما (أ) نعرّف التوكنين في `globals.css` بقيم مقصودة، أو (ب) نستبدلهم بالموجود فعلًا (`--color-foreground` و `--color-text-secondary`). الخيار (ب) أنضف لأنه بيمنع تضخّم نظام التوكنات، لكن (أ) بيحافظ على النية الأصلية لو كان مقصود يبقى فيه درجة أبهت من `text-secondary`.

**✅ تم التنفيذ — القرار: الخيار (ب).** استبدلت `--color-text-muted` بـ `--color-text-secondary` (33 استخدام فعلي عبر نفس الـ6 ملفات — كلها سياقات "باهتة" فعلًا: عناوين أقسام، placeholders، أيقونات، تلميحات) و`--color-text-primary` بـ `--color-foreground` (سياقات نص قوي/أساسي: حقول الإدخال، العناوين، مبالغ الدفعات). مفيش استخدام متبقي للتوكنين الناقصين في المشروع كله.

### 1.2 `--color-primary` مستخدم كلون نص على خلفيات غامقة
`#941414` قرمزي غامق — لون خلفية ممتاز، لكنه يفشل كنص فوق الغامق:

| العنصر | الخلفية | التباين | المطلوب |
|---|---|---|---|
| بادچ الدور `ACCOUNT MANAGER` (8.5px) | `#0D0D0D` | **2.19** | 4.5 |
| بادچ الدور `SUPER ADMIN` (8.5px) | `#0D0D0D` | **2.19** | 4.5 |
| رقم الرسائل غير المقروءة (22px bold) | `#181818` | **2.00** | 3.0 |
| الأڤاتار: ذهبي على قرمزي | `#941414` | 4.21 | 4.5 |

**تأكيد على مستوى الجذر:** البادچ طلع بنفس التباين بالظبط (2.19) مع الدورين المختلفين. يعني المشكلة في التوكن نفسه مش في شاشة أو دور معيّن — **إصلاح واحد في `--color-primary` كلون نص هيحل كل الحالات دفعة واحدة**.

**الحل:** لون نص مخصص للتأكيد على الخلفيات الغامقة (`--color-primary-light` = `#B71C1C` موجود بالفعل وأفتح، أو نضيف درجة أفتح مخصوصة للنص). الأڤاتار حالة حدية — يتصلح بتغميق الخلفية أو تفتيح الذهبي.

**✅ تم التنفيذ.** بدل ما أضيف توكن جديد، لقيت إن `--color-primary-light` نفسه (الموجود بالفعل ومستخدم فعليًا في `ErrorState.tsx` و`LocationPickerModal.tsx`) قيمته القديمة (`#B71C1C`) كانت **برضو فاشلة** كنص فوق خلفيات غامقة (~2.96:1 فوق `#0D0D0D`) — يعني كان فيه باج تاني موجود مسبقًا مستخبي وراه. غيّرت القيمة لـ `#DE4A4A` (محسوبة فعليًا بمعادلة WCAG relative luminance عشان تعدّي 4.5:1 فوق أغمق خلفية في المشروع `#0D0D0D` — طلعت 4.80:1)، وده صلّح تلقائيًا الاستخدامين الموجودين زيادة عن المطلوب.

طبّقت التوكن الجديد في:
- بادچ الدور (`ACCOUNT MANAGER`/`SUPER ADMIN`) في `dashboard/layout.tsx`.
- عداد "الرسائل غير المقروءة" في `DashboardStatCard.tsx` — الخاصية `crimson.val` بس (النص)، مش `crimson.bar` (الخط الزخرفي تحت الرقم، ده خلفية مش نص فمالوش نفس القيد).

الأڤاتار (الحالة الحدية 4.21 مقابل 4.5): بدل ما ألمس `--color-primary` نفسه (مخاطرة أعلى، مستخدم كخلفية في أماكن كتير)، غيّرت لون الحروف الأولى جوه الدائرة من ذهبي لأبيض (`text-white`) في الموضعين (كارت المستخدم بالسايدبار + أڤاتار الهيدر) — أبيض فوق القرمزي بيدّي 8.87:1، الحد الذهبي حوالين الدائرة فضل زي ما هو.

**✅ تحديث — الملاحظة اللي فوق اتحلت.** الفحص الشامل لباقي الـ10 مواضع أكّد المشكلة فعليًا (تباين حسابي ~1.6–1.9:1 لكل منهم تقريبًا، فاشل تمامًا). كماني اتأكد إن `--color-primary-light` الجديد (#DE4A4A، مضبوط على أغمق خلفية `#0D0D0D`) **برضو مش كافي** فوق خلفيات غامقة أفتح شوية زي `#222222`/`#2A2A2A` (~3.5–3.9:1 بس) — التباين بيقل طبيعيًا كل ما الخلفية تفتح مع نفس لون النص. فبدل ما أدوّر على أحمر أفتح تاني (خطر يطلع وردي/كورال مش متوافق مع الهوية)، اخترت نمط تاني: القرمزي فضل بس للحدود/الخطوط الزخرفية (مفيهاش قيد تباين)، والنص اتحول لـ `--color-foreground` (شبه أبيض، ~13-14:1 في كل مكان تقريبًا). اتطبّق في 7 ملفات: `ClientProfileTab.tsx` (أيقونة الموقع اتحولت لـ `--color-primary-light` بدل كده، لأنها أيقونة مش نص فحد الـ3:1 بينطبق مش 4.5)، `ChatTab.tsx`، `ClientChat.tsx`، `ClientPayments.tsx`، `StagesStepper.tsx`، `clients/[id]/page.tsx`، و`client-dashboard/page.tsx` — كلهم تسميات/تابات نصية، مع إبقاء أي حدّ أو خط تحتي قرمزي زي ما هو.

### 1.3 بانرات بألوان الوضع الفاتح على ثيم غامق — **(محتاج تأكيد بصري)**
**18 موضع عبر 8 ملفات** بتستخدم `bg-emerald-50` / `bg-red-50` / `bg-amber-50` / `bg-blue-50` (خلفيات شبه بيضاء) فوق خلفية `#1C1C1C`.

الأخطر: `client-dashboard/page.tsx` سطور 120، 124، 156، 160 — خلفية فاتحة **من غير تحديد لون نص**، فالنص بيورث `--color-foreground` (`#F0F0F0` شبه أبيض) فوق خلفية شبه بيضاء. لو الاستنتاج ده صح فالنص غير مقروء عمليًا.

**الحل — النمط الصح موجود بالفعل في المشروع، مش محتاجين نخترع حاجة:**

`dashboard/finance/page.tsx` بيستخدم الأسلوب السليم للثيم الغامق في كل حالاته:

```
bg-emerald-500/15  text-emerald-400    // معتمدة
bg-amber-500/15    text-amber-400      // معلقة
bg-red-500/15      text-red-400        // مرفوضة
bg-blue-500/15     text-blue-400       // مجدولة
```

خلفية شفافة خفيفة + نص فاتح. الإصلاح = تعميم النمط ده على الـ 18 موضع، ويفضّل نخرجه لمكوّن `<Banner variant="success|error|warning|info">` واحد بدل التكرار.

**✅ تم التنفيذ.** لقيت فعليًا 14 موضع عبر 6 ملفات (مش 18/8 — العدد اختلف شوية عن التقدير الأولي، ممكن يكون فيه تكرار في القراءة الأولى أو مواضع اتصلحت في مرحلة تانية سابقة). استبدلت النمط الفاتح (`bg-*-50 border-*-200 text-*-700`) بنمط الثيم الغامق الموجود بالفعل في `forgot-password`/`reset-password` (`bg-*-500/10 border-*-500/20 text-*-300`):
- `components/clients/ClientProfileTab.tsx`
- `dashboard/settings/page.tsx` (5 مواضع: saved ×2، tax_saved، dates_saved، رسالة بنود العقد)
- `dashboard/clients/[id]/page.tsx` (deleteError)
- `dashboard/profile/page.tsx`
- `client-dashboard/settings/page.tsx`
- `client-dashboard/page.tsx` (5 مواضع، شاملة الأخطر: أسطر 120-162 اللي كانت من غير لون نص محدد)

**لسه برّه النطاق (مقصود):** أزرار اختيار نوع التوقيع في `settings/page.tsx` (`bg-blue-100 text-blue-700`، أسطر 416/420/424) — نمط بصري مختلف (زرار محدد لا بانر تنبيه)، وبوردرات `border-blue-200`/`border-red-200` المستخدمة كحدود أزرار عادية (`account-managers/page.tsx`, `UploadProofModal.tsx`, إلخ) — دي مش جزء من التدقيق الأصلي على البانرات.

**تحقق بعد المرحلة:** `npm test && npm run build && npm run lint` + مرور بصري على الصفحات المتأثرة.

---

## المرحلة 1.5 — تنسيقات متداخلة ومسافات ملتصقة

### 1.5.1 عنوان الصفحة والتاريخ ملزوقين في بعض — `dashboard/layout.tsx:231`

```jsx
<h2 className="text-lg font-bold" style={{ fontFamily: "'Playfair Display', serif" }}>
  {getPageTitle()}
  <span className="text-[11px] ... me-3" style={{ fontFamily: 'Tajawal' }}>
    {currentDate}
  </span>
</h2>
```

**تلات مشاكل متراكبة في نفس العنصر:**

1. **`me-3` على الجانب الغلط.** `me` = `margin-inline-end`، يعني المسافة بتتحط **بعد** التاريخ مش بينه وبين العنوان. المفروض `ms-3` (بداية) — أو الأفضل: حاوية `flex` بـ `gap` عشان تشتغل صح في الاتجاهين من غير تفكير.
2. **التاريخ متداخل جوه الـ `<h2>`.** فالاسم الوصفي للعنوان بيبقى حرفيًا `"SettingsThursday, September 3, 2026"` — ده اللي طلع في استخراج النص من الصفحة الحية. مشكلة تنسيق ومشكلة قارئ شاشة في نفس الوقت.
3. **عائلتين خط متداخلتين جوه عنصر واحد** — `Playfair Display` على الـ `h2` و`Tajawal` مفروضة على الـ `span` جواه.

**الحل:** التاريخ يخرج بره الـ `h2` كعنصر شقيق، والاتنين في حاوية `flex items-baseline gap-3`.

### 1.5.2 مسح باقي حالات الالتصاق
البند اللي فوق اتكشف بالصدفة. لازم مسح مقصود لباقي الصفحات لنفس النمط: عناصر متجاورة من غير `gap`/`margin`، أو استخدام `me-*` مكان `ms-*` والعكس.

`me-*` و `ms-*` دول اتجاهيين (inline-start/end) وسهل جدًا الخلط بينهم في مشروع بيدعم RTL وLTR — فالمسح ده يستاهل يتعمل بجدية على كل الملفات، مش على الهيدر بس.

**✅ جزء منه تم.** مسح `grep` لكل `src` لقى 4 استخدامات متبقية بس لـ `ms-*` (`dashboard/clients/page.tsx`، وثلاثة في `dashboard/finance/page.tsx`) — كلهم صحيحين منطقيًا (مسافة *قبل* عنصر تابع زي `*` أو وحدة عملة "SAR"، والاتجاه ده صحيح في LTR وRTL على حد سواء لأنه بيعتمد على ترتيب القراءة مش جهة الشاشة). مفيش سوء استخدام تاني للـ me/ms غير اللي كان في الهيدر واتصلح في 1.5.1.

**⏸️ الجزء التاني لسه معلّق:** الفحص البصري الحي لعناصر متجاورة ملتصقة من غير `me`/`ms` (زي باج الهيدر بس من غير استخدام هالخاصيتين) محتاج المتصفح على السيرفر شغّال، ومحاولة الوصول لـ `localhost:3000` فشلت وقت التنفيذ (السيرفر مش راد). يتعمل لما السيرفر يرجع شغّال.

---

## المرحلة 2 — سلّم طباعة ومسافات موحّد

### 2.1 مقاسات الخط
**11–12 مقاس مختلف في الصفحة الواحدة**: 8.5, 9.5, 10, 10.5, 11, 11.5, 12, 12.5, 14, 15, 16, 18, 22 px.

(المقاس `11.5px` ظهر بس مع دور `super_admin` — يعني الأحجام بتتولد حسب الشاشة والدور، مش نابعة من سلّم واحد.)

ده مش سلّم — ده قرارات متفرقة. لاحظ 10 مقابل 10.5، و12 مقابل 12.5: فروق نصف بكسل محدش بيشوفها لكنها بتفتّت النظام. و**7 مقاسات تحت 12px**.

**الأوزان: 400 و 700 بس** — مفيش 500/600. يعني التسلسل البصري معتمد كليًا على الحجم واللون، واللون نفسه مكسور (بند 1.1).

**الحل:** سلّم من 5–6 درجات (مثلًا 11 / 12 / 14 / 16 / 20 / 24) كتوكنات، وإضافة وزن 500 أو 600 للعناوين الفرعية. التحويل يتم ملف-ملف مع مقارنة بصرية.

**✅ تم التنفيذ.** الجرد الفعلي طلع أكبر من رقم التقرير الأصلي: **148 استخدام لـ`text-[Npx]`** عبر **30 ملف** (التقرير كان بيتكلم عن عدد الأحجام المختلفة، 11-12، مش عدد الاستخدامات الفردية)، بالإضافة لـ **14 استخدام** لـ `fontSize: N` رقمي جوه `style`/Recharts props في `reports/page.tsx` و`audit-log/page.tsx` مكنتش ظاهرة في الجرد الأصلي خالص.

**السلّم المعتمد** (توكنات `--fs-1` لـ`--fs-6` في `globals.css`، مش `--text-*` — التفاصيل والسبب في تعليق داخل الملف نفسه، وباختصار: أسماء Tailwind الافتراضية `--text-xs/sm/base/lg/xl` مستخدمة فعلًا 719 مرة عبر 52 ملف تاني، وتعريفها كان هيغيّر كل حاجة تانية غير مقصودة):

| توكن | القيمة | بديل عن |
|---|---|---|
| `--fs-1` | 11px | 8.5 / 9.5 / 10 / 10.5px |
| `--fs-2` | 12px | 11.5 / 12.5px |
| `--fs-3` | 14px | (بدون تغيير) |
| `--fs-4` | 16px | 15px |
| `--fs-5` | 20px | 18px |
| `--fs-6` | 24px | 22px |

الاستخدام: `text-[length:var(--fs-N)]` بدل `text-[Npx]` — نفس فكرة `text-[Npx]` بس بتوكن بدل رقم حرفي. طبّقت السلّم على الـ148 موضع كاملة عبر الـ30 ملف.

**استثناء واحد مقصود:** `components/ui/ClientTypeBadge.tsx` كان بيستخدم `10px`/`11px` كفرق حجم متعمّد بين وضعي `compact`/عادي (فرق بكسل واحد بس، لكنه مقصود ومُختبر — فيه تست بيتأكد منه صراحة). لو طبّقت التقريب العادي كانت الحالتين هترجعوا لنفس القيمة (`fs-1` = 11) ويضيع الفرق. بدل كده وزّعتهم على درجتين متتاليتين: `compact` = `--fs-1` (11px)، عادي = `--fs-2` (12px) — يحافظ على النية الأصلية (تصغير واضح) مع الالتزام بالسلّم. حدّثت التست المطابق `ClientTypeBadge.test.tsx` بنفس القيم.

**بند الأوزان (500/600):** فحصت الاستخدام الفعلي قبل ما أضيف حاجة، ولقيت `font-medium`/`font-semibold` مستخدمين بالفعل **184 مرة عبر 46 ملف** — يعني ادعاء التقرير الأصلي ("الأوزان 400 و700 بس") مش دقيق، على الأقل مش بالمعنى الحرفي. النظام فعليًا عنده تدرّج أوزان مستخدم فعلًا. معملتش تغيير هنا لتجنب فرض وزن على عناصر بمنطق بصري ما قدرتش أتأكد منه بصريًا.

**محتاج تحقق دقيق بصريًا خصوصًا هنا** — ده أكبر تغيير بصري في اليوم ده (148 موضع)، وأصغر 4 أحجام كبرت من نص بكسل لحد بكسل ونص. البادچات والتسميات الصغيرة في الجداول والسايدبار هي الأكتر تأثرًا.

### 2.2 أنصاف الأقطار والحشو
4 قيم radius حقيقية (3، 8، 10، 12px) + `rounded-full`. و9 قيم padding (1، 6، 8، 10، 12، 14، 16، 20، 24px) — الغالب فيها 10px و14px وهي درجات Tailwind الفردية (2.5 و3.5).

**الحل:** تثبيت 3 قيم radius (صغير/متوسط/كبير) وسلّم حشو من مضاعفات 4.

---

## المرحلة 3 — تقليل التكرار في التنسيق

### 3.1 `Playfair Display` inline — 38 مرة في 16 ملف
`style={{ fontFamily: "'Playfair Display', serif" }}` مكرر حرفيًا 38 مرة.

**الحل:** توكن خط واحد في `@theme` (مثلًا `--font-display`) وكلاس `font-display`، واستبدال الـ 38 موضع. نفس أسلوب توحيد `resolveFileUrl` اللي اتعمل قبل كده في المشروع.

**✅ تم التنفيذ.** أضفت `--font-display: 'Playfair Display', serif;` جوه `@theme inline` في `globals.css` — Tailwind v4 بيولّد منها كلاس `font-display` تلقائيًا. استبدلت كل الـ 36 موضع (اللي لقيتهم فعليًا عبر 15 ملف، مش 38/16 المقدّرة — فرق بسيط) بالكلاس، شاملة موضعين كانوا جوه `<style jsx>` (CSS خام) في `reports/page.tsx` واتحولوا لـ `font-family: var(--font-display)`. مفيش `style={{ fontFamily: ... }}` باقي في المشروع غير تعريف التوكن نفسه.

### 3.2 83 لون hex مكتوب يدويًا برّه نظام التوكنات
مركّزين في صفحات المصادقة (login 11، client-login 15، forgot-password 10، reset-password 13 = **49**)، وaudit-log 12، reports 12.

يعني صفحات الـ auth عايشة برّه نظام التصميم بالكامل — أي تغيير في الهوية مش هيوصلها.

**تمييز مهم — مش كل الـ 83 نفس الدرجة:**

- **تكرار صريح لقيم موجودة كتوكنات:** في `reports/page.tsx` مثلًا `#22C55E` هو `--color-success` بالظبط، `#EF4444` هو `--color-error`، `#EAB308` هو `--color-warning`، `#D4AF37` هو `--color-gold`، `#606060` هو `--color-text-disabled`. دي أولوية إصلاح واضحة.
- **ألوان مش موجودة في النظام أصلًا:** `#60A5FA`، `#A78BFA`، `#FB923C` (ألوان حالات العقود)، و`#C0C0C0`/`#CD7F32` (فضي/برونزي لترتيب المتصدرين). دي محتاجة قرار: تتضاف للنظام كتوكنات حالات، ولا تفضل محلية.
- **عذر مشروع جزئيًا:** ألوان الـ `fill` اللي بتتبعت لـ Recharts — تمرير CSS variables ليها مش مضمون في كل الحالات. لو اتقرر تفضل hex، تتجمّع في مكان واحد مع تعليق يشرح السبب بدل ما تبقى متناترة.

**✅ تم التنفيذ — القرار: الألوان الجديدة اتضافت كتوكنات.** أضفت 5 توكنات جديدة في `globals.css` (`--color-status-blue`, `--color-status-purple`, `--color-status-orange`, `--color-rank-silver`, `--color-rank-bronze`) للألوان اللي مالهاش مطابقة في النظام. استبدلت كل الـ hex اللي بيطابق توكن موجود (قديم أو جديد) بـ `var(--token)` في **9 ملفات**: `reports/page.tsx`، `audit-log/page.tsx`، `login/page.tsx`، `client-login/page.tsx`، `forgot-password/page.tsx`، `reset-password/page.tsx`، `dashboard/layout.tsx` (كان بالفعل متبع للتوكن)، `PaymentsTab.tsx`، `ClientPayments.tsx`.

القيم اللي **اتسابت hex عمدًا** (مش نسيان):
- **`STATUS_COLORS` و`approvalData` في `reports/page.tsx`** (6 مواضع) — دول بيتبعتوا مباشرة لـ `<Cell fill={...}/>` في Recharts، وده بيرندرها كـ SVG attribute مش CSS style، فموثوقية `var()` معاها مش مؤكدة بصريًا من غير تجربة حية (والسيرفر مش راد وقت التنفيذ). سبتها hex زي ما كانت بس ضفت تعليق فوقها بيوضح إنها بتطابق التوكنات. أي مكان تاني بيستخدم نفس القيمة عبر `style={{}}` عادي (زي `valueColor` والـ`styled-jsx` blocks) اتحول لـ `var()` بأمان.
- **`#1E1E1E`** (كروت صفحات الـ auth الأربعة) **و`#7a1010`** (hover لزرار الدخول) — مفيش توكن مطابق تمامًا ليهم في النظام الحالي (أقرب حاجة `--color-card` #222222 مش نفس القيمة). سبتهم عمدًا بدل ما أعمل تقريب قد يغيّر الشكل البصري من غير ما أقدر أتأكد بصريًا.
- **`#111111` في `showcase-demo/page.tsx`** — صفحة عرض داخلية، مفيش توكن مطابق، أولوية منخفضة.
- **`ctx.fillStyle = '#ffffff'` في `ClientSignature.tsx`** — Canvas API، مش CSS، لازم يفضل قيمة حرفية.
- **3 ألوان في `MeetingChip.tsx`** (`#1a5276`, `#0d2137`, `#5dade2`) — لوحة ألوان منفصلة غير مطروحة في القرار الأصلي، سبتها من غير قرار مسبق.

**النتيجة:** من الـ83 استخدام الأصلي، **61 اتحول لتوكنات**، والـ22 الباقيين موثّقين فوق بسبب واضح لكل واحد.

### 3.3 توكنات متضاربة في `globals.css`
أزواج بتوصف نفس الدور بأسماء مختلفة: `--color-card` (#222) مقابل `--color-card-bg` (#181818) · `--color-card-border` مقابل `--border` (نفس القيمة بالظبط) · `--color-sidebar` مقابل `--bg-dark` (نفس القيمة) · `--color-error` مقابل `--color-red-accent` · `--color-success` مقابل `--color-green` · `--color-info` مقابل `--color-blue`.

**الحل:** اختيار اسم واحد لكل دور، والقديم يفضل موجود كـ alias لحد ما كل الاستخدامات تتحوّل، وبعدين يتشال.

**✅ تم التنفيذ جزئيًا — واكتشفت إن نص البند مش دقيق لكل الأزواج.** قبل ما أعمل أي alias رحت اتأكدت من القيمة الفعلية لكل توكن في `globals.css`، ولقيت الأزواج مش كلها بتحمل نفس القيمة زي ما ذكر النص:

**زوجين فعلًا بنفس القيمة بالظبط — دول اتحدوا بأمان (صفر تغيير بصري):**
- `--color-card-border` و`--border` (الاتنين `#2A2A2A`) — `--border` بقى الأساسي (مستخدم 45 مرة لوحده + دلالته أعم من "حدود الكارت")، و`--color-card-border` بقى `var(--border)`.
- `--color-sidebar` و`--bg-dark` (الاتنين `#0D0D0D`) — `--bg-dark` بقى الأساسي (مستخدم فعليًا للسايدبار الحقيقي في `dashboard/layout.tsx` + صناديق تانية غامقة، بينما `--color-sidebar` مستخدم مرة واحدة بس في `showcase-demo`)، و`--color-sidebar` بقى `var(--bg-dark)`.

**تلات أزواج التانية — القيم مختلفة فعليًا، سبتها منفصلة ومحتاجة قرارك:**
- `--color-card` (`#222222`) مقابل `--color-card-bg` (`#181818`) — مش نفس اللون. `--color-card-bg` مستخدم بانضباط في 12 موضع كلهم "كروت مستوى صفحة" (إحصائيات الداشبورد، قوائم AM/SA، `LoadingSkeleton`) دايمًا مقرون بـ `--border`. `--color-card` مستخدم في 89 موضع للكروت الداخلية/المودالز. النمط ده يشبه نظام "سطحين" (surface/raised-surface) مقصود، مش تكرار بالغلط.
- `--color-error` (`#EF4444`) مقابل `--color-red-accent` (`#E24B4A`) و`--color-success` (`#22C55E`) مقابل `--color-green` (`#97C459`) و`--color-info` (`#3B82F6`) مقابل `--color-blue` (`#85B7EB`) — تلاتتهم نفس النمط: الأول (`error`/`success`/`info`) مستخدم لحالات/بادچات صريحة، والتاني (`red-accent`/`green`/`blue`) مستخدم في مجموعة محدودة من الملفات (`ActivityFeed.tsx`, `DashboardStatCard.tsx`, `ManagerTableRow.tsx`, رسوم `reports`) كلوحة ألوان أخف/أهدأ لمؤشرات الاتجاه (سهم لأعلى/لأسفل) وبادچات نوع المدير — مش استخدام عشوائي أو تكرار غلط.

**دمج الأزواج دي زي ما هي هيغيّر الألوان الفعلية المعروضة** (لأن القيم مختلفة، مش نفس اللون باسمين) **من غير ما أقدر أتأكد بصريًا** (السيرفر لسه مش راد). فضّلت "الكود القديم يفضل شغّال لحد ما البديل يثبت إنه شغّال" وسبتهم منفصلين. لو تحب تدمجهم فعلًا (يعني كل الحالات دي ترجع للون واحد)، محتاج قرار صريح مع العلم إنه هيغيّر شكل بعض العناصر فعليًا.

**اكتشاف جانبي أثناء الفحص — باج حقيقي:** `dashboard/reports/page.tsx:300` كان بيستخدم `var(--color-red)` وده توكن **مش معرّف إطلاقًا** في `globals.css` (نفس فئة باج 1.1). صلّحته لـ `var(--color-red-accent)` عشان يطابق شريكه `--color-green` في نفس السطر (مؤشر اتجاه لأسفل/لأعلى).

---

## المرحلة 4 — لمسات نهائية

### 4.1 أهداف لمس أصغر من الحد الأدنى (24×24)
زرار `Logout` ارتفاعه **17px** · مبدّل اللغة **16px** · روابط `View All` / `Back to Dashboard` / `Manage` **16px**.

الارتفاعات دي ثابتة عبر كل الصفحات والدورين — يعني مصدرها الـ layout المشترك، فالإصلاح مركزي (زيادة `py` على عناصر الهيدر والروابط النصية).

**✅ تم التنفيذ.** استخدمت تقنية `py-* -my-*` (padding يوسّع منطقة اللمس + margin سالب يلغي أثره على المسافات المحيطة) عشان ما نغيّرش شكل الـ layout بصريًا:
- `dashboard/layout.tsx`: زرار `Logout` ومبدّل اللغة.
- `components/dashboard/AMView.tsx`: رابط `view_all`.
- `components/dashboard/PaginatedView.tsx`: رابط `paginated_back`.
- `components/dashboard/SAManagersView.tsx`: رابط `manage`.

كلهم جوه حاويات `items-center`، فالتقنية آمنة ومتسّقة.

### 4.1.b فشل صامت: انقطاع الـ API بيبان كأنه "مفيش بيانات"
**اتكشف عمليًا أثناء المراجعة** (مش استنتاج نظري): لما مسارات `/api/proxy/*` رجعت 404، الداشبورد عرض **قوايم فاضية من غير أي رسالة خطأ** — نفس شكل "مفيش عملاء". صاحب المشروع افتكر فعلًا إن العميل اللي ضايفه اختفى.

السبب في الكود: `.catch(() => ({ data: null }))` و`.catch(() => [])` في عدد من نقاط الجلب، بتبلع الخطأ وترجع فراغ. النتيجة إن حالة "فشل الاتصال" وحالة "لا توجد بيانات" مش متميزين بصريًا.

**الحل:** التمييز بين الحالتين — فشل الطلب يعرض `ErrorState` (المكوّن موجود بالفعل) مع إمكانية إعادة المحاولة، والفراغ الحقيقي بس هو اللي يعرض `EmptyState`.

هو باج سلوكي أكتر منه بصري، لكنه بيأثر مباشرة على ثقة المستخدم في البيانات، فمكانه هنا.

**✅ تم التنفيذ.** الملفات اللي كانت بتبلع خطأ التحميل الأول وتتصرف كأن مفيش بيانات، وأضيف لها `ErrorState` + إعادة محاولة:
- `dashboard/clients/page.tsx`
- `components/payments/PaymentsTab.tsx` (+ `hasLoadedOnceRef` عشان الـ polling)
- `components/files/FilesTab.tsx`
- `components/calendar/CalendarTab.tsx`
- `components/chat/ChatTab.tsx` (+ `hasLoadedOnceRef` عشان الـ polling والـ websocket)
- `dashboard/settings/page.tsx` (تحميل بنود العقد بس — بروفايل المستخدم وإعدادات الضريبة بترجع لقيم افتراضية معقولة عند الفشل مش لحالة "فاضي" مضلِّلة، فمحتاجتش نفس المعالجة)

الملفات التانية اللي فيها `.catch()` كانت بالفعل صح (مش بگ) أو catches خاصة بعمليات حفظ/تعديل (مش تحميل بيانات)، فاتسابت زي ما هي:
`ApprovalsTab` / `ContractsTab` / `MeetingsTab` / كل مكوّنات `client-*` / `ContractBuilder.tsx` (فشل تحميل القوالب فيه بيسيب القسم يختفي بدل ما يبان فاضي بشكل مضلِّل — أثر أقل، اتسيب لمرحلة تانية لو لزم) / `UploadProofModal.tsx` (catch خاص برفع الملف، مش تحميل).

**محتاج تحقق منك:** `npm test && npm run build && npm run lint` في `shadapp-dashboard` قبل ما نكمل للبند اللي بعده.

### 4.2 حالة تحميل غير متسقة
`LoadingSkeleton` — الأكثر استخدامًا (14+ ملف: كل تابات تفاصيل العميل ونظائرها عند العميل) — **مش skeleton أصلًا**، ده نص رمادي في المنتصف مكتوب فيه "جاري التحميل".

في نفس الملف بالظبط فيه `DashboardSkeleton` و `ReportsSkeleton` و `TableSkeleton` — skeletons متحركة حقيقية، ومستخدمة في الصفحات الرئيسية بس.

يعني البنية التحتية موجودة ومتسّتة بالفعل. التابات الداخلية بس هي اللي واخدة النسخة النصية.

**الحل:** استخدام `TableSkeleton` / skeleton مناسب في التابات الداخلية. مجهود قليل ومكسب واضح.

**✅ تم التنفيذ.** استبدلت `<LoadingSkeleton />` بـ `<TableSkeleton />` في 15 تاب/مكوّن داخلي: `ClientSubUsers` / `ClientChat` / `ChatTab` / `ClientContracts` / `ClientApprovals` / `ClientMeetings` / `CalendarTab` / `ClientPayments` / `ClientFiles` / `ApprovalsTab` / `ContractsTab` / `FilesTab` / `MeetingsTab` / `ClientProfileTab` / `PaymentsTab` (استيراد + استخدام في كل ملف). في `ClientChat.tsx` شلت الخاصية `message={t('chat_loading')}` بما إن `TableSkeleton` مالهاش prop للرسالة — اتأكدت إن `t` لسه مستخدم 19 مرة تانية في الملف فمفيش unused-var.

اتسابت عمدًا 3 ملفات على `LoadingSkeleton` الأصلي لأنها full-screen loaders على مستوى الصفحة (سياق UX مختلف عن التابات الداخلية، مش سكيلتون جدول): `app/client-dashboard/page.tsx`، `app/dashboard/clients/[id]/page.tsx`، `app/dashboard/clients/[id]/settings/page.tsx`. اتأكدت بـ grep نهائي إن مفيش أي استخدام لـ `LoadingSkeleton` متبقي خارج الثلاث دول وملف التعريف/الاختبار الخاص بيه.

### 4.3 `td.p-4 { text-align: center }` في `globals.css`
قاعدة عامة مربوطة بكلاس Tailwind — بتوسّط أي خلية جدول فيها `p-4` في المشروع كله سواء كان ده المقصود أو لأ. هشّة ومخفية.

**✅ تم التنفيذ — ولقيت باج حقيقي بسببها.** القاعدة دي (specificity 0,1,1) كانت بتغلب على `.text-end` (specificity 0,1,0) بسبب أولوية CSS. النتيجة الفعلية: عمود أزرار الإجراءات في `dashboard/clients/page.tsx` (سطر 287) كان مكتوب عليه `text-end` بقصد واضح من المطوّر، لكنه كان بيترندر **متمركز** فعليًا بسبب القاعدة العامة — باج صامت حقيقي، مش افتراض نظري.

الحل: حذفت القاعدة من `globals.css`، وأضفت `text-center` صراحة لكل `<td>` في الجدولين الوحيدين اللي بيستخدموا `p-4` (`dashboard/clients/page.tsx`، `dashboard/account-managers/page.tsx`) بحيث يطابقوا نية رؤوس الأعمدة (`<th className="text-center">`) المكتوبة فعلًا. عمود الإجراءات في `clients/page.tsx` سطر 287 اتسيب زي ما هو (`text-end`) — دلوقتي هيشتغل صح لأول مرة.

### 4.4 تحميل الخطوط
`@import` من Google Fonts داخل CSS: **4 عائلات و17 وزن**. الفعلي المستخدم على الصفحة: `Tajawal` (82 عنصر) و `Playfair Display` (7). `Archivo` و `Noto Sans Arabic` احتياطيان بس وبنحمّل منهم 9 أوزان.

**الحل:** `next/font` بدل `@import` (بيمنع تعطيل الرندر)، وتقليل الأوزان للمستخدم فعليًا.

**✅ تم التنفيذ.** أنشأت `src/lib/fonts.ts` بتحمّل `Tajawal` (400/500/700، عربي+لاتيني) و`Playfair Display` (400/500/600/700 + إيطاليك 400 — مستخدم فعليًا في شعار "d") عبر `next/font/google`، وشيلت `Archivo` و`Noto Sans Arabic` نهائيًا لأنهم كانوا احتياطيين بس ومحدش بيستخدمهم كخط مسمّى صراحة في أي مكان بالكود.

طبّقت المتغيرين (`--font-tajawal`، `--font-playfair`) على `<html>` في كل من `layout.tsx` (الجذر) و`global-error.tsx` (بيرندر `<html>` خاص بيه منفصل — الاتنين بيستوردوا من نفس `lib/fonts.ts` عشان يفضلوا متزامنين). حذفت الـ `@import` من `globals.css`، وحدّثت `--font-display` و`body`'s `font-family` ليشيروا لـ `var(--font-playfair)`/`var(--font-tajawal)` بدل الاسم الحرفي.

**نقطة مهمة اكتشفتها أثناء التنفيذ:** next/font بيولّد اسم خط داخلي (مش حرفيًا "Tajawal")، فأي مكان في الكود كان بيكتب `fontFamily: 'Tajawal'` صراحةً (مش عن طريق التوكن) كان هيفشل بصمت ويرجع لخط النظام الافتراضي بعد التحويل. لقيت **9 مواضع كده**: 7 في رسوم `reports/page.tsx` (Recharts tick/tooltip styles) و2 في `dashboard/layout.tsx` (التاريخ في الهيدر وحقل البحث) — استبدلتهم كلهم بـ `fontFamily: 'var(--font-tajawal)'`. من غير الفحص ده كان الخط هيتغيّر بصمت في 9 أماكن.

**✅ تحقق أخضر من المستخدم (بند 1.1 + 1.3 + 4.3 + 4.4 مجتمعين):** `npm test` (132/132 ✅) → `npm run build` (نجح، كل الصفحات ✅) → `npm run lint` (0 أخطاء، 162 تحذير — كلها موجودة من قبل ومش متعلقة بالتغييرات دي: `no-explicit-any`، `no-img-element`، `set-state-in-effect` على أنماط `load()` موجودة أصلًا في مكونات تانية زي `CalendarTab`/`FilesTab`، متغيرات غير مستخدمة. مفيش تحذير جديد راجع لتغييرات الخطوط أو الألوان أو `td.p-4`).

---

## المرحلة 4.5 — الثيم الفاتح (فيتشر جديد)

**دي مش إصلاح — دي ميزة جديدة، وأكبر بند في الخطة.** ومتوقفة كليًا على إنجاز المراحل 1 و2 و3 الأول.

### ليه لازم تيجي في الآخر
`globals.css` دلوقتي بيعرّف قيم غامقة **ثابتة** جوه `@theme` — مفيش `prefers-color-scheme`، مفيش `data-theme`، ومفيش طبقة تبديل أصلًا. يعني بناء ثيم فاتح مش إضافة ملف، ده إعادة هيكلة لطبقة الألوان كلها.

والأهم: **البنود اللي قبلها هي شروط مسبقة فعلية:**

- **بند 1.1 (التوكنات الناقصة):** `--color-text-muted` و`--color-text-primary` مش معرّفين. لو بنينا ثيم فاتح وهما ناقصين، النصوص دي هتورث لون الثيم الفاتح وتبقى غير مقروءة — نفس الباج بس بشكل أسوأ.
- **بند 3.2 (83 لون hex):** أي لون مكتوب يدويًا مش هيتغير مع الثيم. صفحات الـ auth الـ 49 لون هتفضل غامقة في الوضع الفاتح.
- **بند 3.3 (التوكنات المتضاربة):** لازم اسم واحد لكل دور قبل ما نعمل نسختين من كل اسم.

يعني الترتيب مش تفضيل تنظيمي — لو اتعكس، الثيم الفاتح هيتبني على أساس مكسور.

### الخطوات المقترحة
1. **إعادة هيكلة التوكنات لطبقتين:** توكنات دلالية (`--color-surface`، `--color-text-primary`، `--color-border`...) منفصلة عن القيم الخام. الشيفرة تستخدم الدلالي بس.
2. **تعريف مجموعتي قيم** تحت `:root` (فاتح) و`[data-theme="dark"]` أو العكس، حسب الافتراضي المطلوب.
3. **مبدّل ثيم** يحفظ الاختيار ويحترم `prefers-color-scheme` كافتراضي أول مرة.
4. **مراجعة كل حالة لون بالثيمين** — خصوصًا البانرات (بند 1.3)، ألوان الحالات، وألوان الرسوم البيانية في `reports`.
5. **إعادة تشغيل فاحص التباين على الثيمين** — القيم اللي تعدّي في الغامق مش بالضرورة تعدّي في الفاتح، والعكس.

### قرار مطلوب
~~الافتراضي يبقى إيه — فاتح، غامق، ولا يتبع إعداد نظام المستخدم؟~~ **✅ قرار: غامق دائمًا كافتراضي، مفيش متابعة لـ prefers-color-scheme.**

### ✅ تم التنفيذ — دفعة 1 (الآلية + التوكنات الأساسية)

**القرارات المعمارية (بموافقة المستخدم):**
- السايدبار (والهيدر معاه) بيفضلوا غامقين دايمًا مهما الثيم المختار — زي Notion/Slack. بس منطقة `<main>` (المحتوى) هي اللي بتحترم الثيم.
- الافتراضي غامق دائمًا حتى بعد إضافة الفاتح — مفيش `prefers-color-scheme` أول زيارة، بس الاختيار بيتحفظ في `localStorage` (`shadapp-theme`) بعد أول تبديل يدوي.
- التطبيق عبر `[data-theme="light"]` متطبّق على `<main>` بس (مش `<html>`) — بفضل إن كل مكوّنات المحتوى أصلًا بتستخدم `var(--color-*)` (نتيجة بند 3.2/3.3)، التوكنات الجديدة بتورث تلقائيًا لكل حاجة جوه `<main>` من غير أي لمس في أي مكوّن.

**الملفات:**
- `globals.css`: بلوك `[data-theme="light"]` بيعرّف `--color-background` (`#F0F0F0`، Soft White من دليل الهوية)، `--color-foreground` (`#1C1C1C`، Charcoal Black)، `--color-card` (`#FFFFFF`)، `--color-card-bg` (`#FAFAFA`)، `--border` (`#DADADA`)، `--color-input-fill` (`#E8E8E8`)، `--color-input-border` (`#C7C7C7`)، `--color-text-secondary` (`#5A5A5A`)، `--color-text-disabled` (`#8A8A8A`).
- `dashboard/layout.tsx`: زرار تبديل (أيقونة Sun/Moon من lucide-react) في الهيدر جنب جرس الإشعارات، حالة `theme` بتتحفظ في localStorage، `data-theme` متطبّق على `<main>`.
- `messages/ar.json` و`en.json`: مفاتيح `switch_to_light`/`switch_to_dark` لـ aria-label الزرار.

**تحقق التباين (حسبت WCAG فعليًا، مش تخمين — مفيش معاينة بصرية حية متاحة):**
- نص `#1C1C1C` فوق خلفية `#F0F0F0`: **14.96:1** (ممتاز).
- `--color-text-secondary` الجديد فوق الخلفية الجديدة: **6.05:1** (يعدّي AA).
- `--color-text-disabled` الجديد: **3.03:1** — قريب من نظير الثيم الغامق نفسه (2.71:1)، متسق مع كونه نص "معطّل" مش نص أساسي.
- القرمزي `--color-primary` كنص فوق الخلفية الفاتحة: **7.78:1** (أفضل بكتير من الغامق، مفيش تعديل لازم له).
- الأزرار (أبيض فوق قرمزي، أسود فوق ذهبي): مستقلة عن خلفية الصفحة، مفيش تغيير لازم.

**لسه برا النطاق (دفعة 1):** صفحات الـ auth (`login`/`forgot-password`/إلخ) وبوابة العميل (`/client-dashboard`) — مالهمش سايدبار بنفس البنية، مش متضمّنين في دفعة 1.

### ✅ تم التنفيذ — دفعة 2 (تصنيف `--color-gold` كنص)

**المشكلة:** `--color-gold` (`#D4AF37`) مستخدم بشكل مزدوج — خلفية زرار (محتاجة تفضل فاتحة، مستقلة عن ثيم الصفحة) ولون نص (محتاج يبقى غامق فوق الخلفية الفاتحة الجديدة). حسبت التباين الفعلي: الذهبي كنص فوق `#F0F0F0` بيدّي **1.85:1** بس — فاشل تمامًا مقابل حد الـ4.5:1.

**الحل المعماري (زيرو-مخاطرة):** توكن جديد `--color-gold-text` قيمته **مطابقة تمامًا** لـ `--color-gold` في الثيم الغامق الافتراضي (`#D4AF37` — زيرو تغيير بصري في أي مكان لسه شغال بالغامق)، ويتغيّر بس تحت `[data-theme="light"]` لقيمة أغمق محسوبة فعليًا (`#7A5A12`، بتدّي 6.37:1 فوق `--color-card` الأبيض و5.59:1 فوق `--color-background`). بما إن القيمة زيرو-فرق في السياق الحالي، أمكن عمل مسح ميكانيكي عبر الكود كله من غير خطر رجعة بصرية.

**التصنيف:** فحصت الاستخدامات الفعلية (اتضح 117 موضع "نص" من إجمالي الـ192 اللي كانت مقدّرة أول مرة — الفرق لأن جزء من الـ192 كان استخدامات bg-/border- مش نص، ودي متروكة عمدًا لبرا نطاق دفعة 2). استبدلت كل `text-[var(--color-gold)]` بـ `text-[var(--color-gold-text)]` عبر **38 ملف**، شاملة `NotificationBell.tsx`، `reports/page.tsx` (7 مواضع)، `PaymentsTab.tsx`، `dashboard/layout.tsx` (مع استثناء متعمّد لموضعين — شعار "SHAD" وhover زرار تبديل اللغة — لأنهم جوه السايدبار/الهيدر الدائم الغمقان، برا نطاق `[data-theme="light"]` أصلًا). استخدامات bg-/border- الذهب (19+34 موضع تقريبًا) اتسيبت عمدًا زي ما هي — دي خلفيات زرار/حدود، مش نص، مالهاش نفس قيد التباين.

**⚠️ لسه مؤجّل (دفعة 3 محتملة):** ~~ألوان الحالة (success/warning/error/info/purple) ممكن يكون عندها نفس نمط الاستخدام المزدوج — لسه معملتش فيها فحص.~~ **✅ اتفحصت، شوف دفعة 3 تحت.**

### ✅ تم التنفيذ — دفعة 3 (success/error كنص + إكمال دفعة 2)

**الفحص:** `--color-warning` و`--color-info` و`--color-purple` **مفيش أي استخدام فعلي لهم كنص** في الكود كله (بس خلفيات/حدود/رسوم بيانية) — مش محتاجين توكن مقابل. `--color-purple` مستخدم مرة واحدة كـ`fill` لعمود رسم بياني (`reports/page.tsx:448`)، وده عنصر رسومي لا نص، وتباينه فعليًا 3.71-4.23:1 فوق الخلفيات الفاتحة — عادي لأن حد العناصر الرسومية 3:1.

**`--color-success` و`--color-error` كانوا فعليًا بنفس مشكلة الذهبي:** حسبت التباين فعليًا — الأخضر `#22C55E` بيدّي ~2.0:1 فوق `#F0F0F0`، والأحمر `#EF4444` بيدّي ~3.3:1 — الاتنين تحت حد الـ4.5:1 للنص العادي. أضفت `--color-success-text` (`#166534` في الفاتح، تباين 6.26:1) و`--color-error-text` (`#B91C1C` في الفاتح، تباين 5.68:1)، بنفس مبدأ `--color-gold-text` (زيرو تغيير في الغامق). طبّقتهم في 3 مواضع نص فعلية: `ClientPayments.tsx` (تسمية "مدفوع بالكامل" + الإيموجي جنبها)، و`audit-log/page.tsx` (لون افتراضي الأڤاتار + كلاس `.ab-client` لبادچ نوع الحدث).

**اكتشاف جانبي مهم:** فحص `--color-error` عند `reports/page.tsx:177` (قيمة KPI "الموافقات المعلّقة"، `text-[fs-6] font-bold` = 24px bold) طلع **عادي فعلًا** — بما إنه نص كبير (حد 3:1 مش 4.5:1)، والأحمر الخام بيدّي 3.3-3.76:1 حسب الخلفية، فبيعدّي الحد. سبته من غير تغيير.

**اكتشاف جانبي تاني — إكمال حقيقي لدفعة 2 (مش دفعة 3):** أثناء الفحص لقيت إن سويپ الذهب في دفعة 2 كان بيدوّر بس على نمط Tailwind (`text-[var(--color-gold)]`) وفاته استخدامات "نص" تانية مكتوبة كـ *JS string* جوه كائنات ألوان أو `style={{color: ...}}` مباشر — نفس التوكن، نفس المشكلة، بس نمط بحث مختلف. صلّحتها في 4 ملفات:
- `audit-log/page.tsx`: 3 مواضع (لوحة ألوان الأڤاتار مرتين، وكلاس `.ab-payment`).
- `ManagerTableRow.tsx`: مواضع الأڤاتار الاتنين (كارمزي وذهبي) + بادچ العداد الافتراضي.
- `DashboardStatCard.tsx`: قيمة `gold.val` (نفس نمط `crimson.val` بالظبط في بند 1.2 — نص 24px bold فوق `--color-card-bg`).

**باج حقيقي منفصل اتكشف واتصلح بالمرة (`ManagerTableRow.tsx`):** بادچ العداد الافتراضي كان بيستخدم `var(--gold-soft)` (من غير بادئة `--color-` الصحيحة) — توكن **غير معرّف إطلاقًا**، يعني الخلفية كانت بتتجاهل بصمت في كل الأحوال (مش مرتبط بالثيم الفاتح، باج موجود من الأول). صلّحته لـ `var(--color-gold-soft)` الصح.

**✅ تم التنفيذ — دفعة 4 (توكنات الحالة الإضافية كنص).** ~~مؤجّلة، محتاجة قرار~~ — اتنفّذت بموافقتك.

**الفحص:** كل التوكنات دي (`--color-status-purple/blue/orange`, `--color-green`, `--color-blue`, `--color-rank-silver/bronze`) طلعت **أسوأ** من الذهب/الأخضر-الأساسي/الأحمر-الأساسي (اللي اتصلحوا في دفعة 2/3) — كلها ألوان "باستيل" مصممة أصلًا للثيم الغامق، فحسابيًا كنص فوق `#F0F0F0` بتدّي بين 1.5:1 و2.8:1 (حتى تحت حد الـ3:1 للنص الكبير/العناصر الرسومية، مش بس 4.5:1 العادي). أضفت 7 توكنات `-text` جديدة بنفس مبدأ الدفعات اللي فاتت (زيرو تغيير في الغامق، قيمة أغمق محسوبة فعليًا تحت `[data-theme="light"]` — تباين 5.7-9:1 حسب اللون).

**الملفات:**
- `audit-log/page.tsx`: لوحة ألوان الأڤاتار (status-purple/blue/orange)، رابط "عرض" (status-blue)، كلاسات `.ab-contract/.ab-approval/.ab-login/.ab-file/.ab-workspace`.
- `reports/page.tsx`: تريند "▲/▼" (green)، عمود الرسم البياني الثالث في الـleaderboard (blue — عنصر رسومي، حد 3:1)، صفوف الترتيب `.lb-rank.r2/.r3` (rank-silver/bronze).
- `ManagerTableRow.tsx`: أڤاتار اللون التالت (blue)، بادچ العداد الافتراضي (green).

**اكتشاف جانبي — إكمال حقيقي لدفعة 2 (مش دفعة 4):** طريقة البحث في دفعة 2 كانت بتدوّر بس على أنماط بعلامات اقتباس (`'var(--color-gold)'`) وفاتت قواعد CSS خام جوه `<style jsx>` من غير اقتباس (`color: var(--color-gold);`). لقيت وصلّحت 3 مواضع كده في `reports/page.tsx`: `.lb-rank.r1` (بادچ الترتيب الأول)، `.lb-av` (أڤاتار الـleaderboard)، `.lb-val` (قيمة الإيراد) — الثلاثة كانوا لسه على `--color-gold` الخام.

**اكتشاف جانبي تاني — توكن مش في أي دفعة سابقة:** أثناء إصلاح تريند "▼" في `reports/page.tsx:300` لقيت جاره "▲" (`--color-red-accent`) بيفشل برضو (3.45:1 فوق `#F0F0F0` — يعدّي حد النص الكبير 3:1 بس مش حد النص الصغير 4.5:1 اللي هو مستخدم فيه هنا فعليًا). أضفت `--color-red-accent-text` وطبقته في الموضع ده بس. باقي استخدامات `--color-red-accent` (نقطة `ActivityFeed` كخلفية، و`DashboardStatCard.red.val` كنص 24px bold) اتفحصت ولقيتها سليمة (خلفية مش نص، أو نص كبير بيعدّي حد الـ3:1 فعليًا) فسبتها من غير تغيير.

**كل bg-/border- والعناصر الزخرفية البحتة** (نقاط `ActivityFeed`، خطوط `.kpi.*::after` تحت الأرقام) **اتسابت عمدًا** — نفس مبدأ كل الدفعات اللي فاتت.

---

## المرحلة 5 — تحقق نهائي
- ~~`npm test && npm run build && npm run lint` كامل، ومقارنة بخط الأساس.~~ **✅ شغّلها المستخدم بنفسه:** 132/132 تست ناجح (24 ملف)، بناء ناجح تمامًا (22 صفحة)، لينت 162 تحذير لكن **صفر أخطاء** — كل التحذيرات نمط قديم موجود من الأول (react-hooks/exhaustive-deps، set-state-in-effect، no-explicit-any، no-img-element) ومفيهاش أي حاجة جديدة متعلقة بتعديلات الجلسة دي (الثيم الفاتح، توكنات النص، إصلاحات الخطوط). خط الأساس التقني سليم.
- **✅ إعادة التحقق بعد دفعة 4:** المستخدم شغّل الفحص تاني بعد إضافة توكنات الحالة الإضافية (`-text` لـstatus-blue/purple/orange، green، blue، rank-silver/bronze، red-accent) — **الكل أخضر**، مفيش أي كسر.
- ⏸️ مرور بصري على كل الصفحات على 1440px و768px و375px — لسه محتاج السيرفر شغّال حيًا.
- ⏸️ **تسجيل دخول بحساب `super_admin`** لتغطية الشاشات اللي ما اتفحصتش حيًا (الإعدادات الكاملة، finance، reports) وواجهة العميل — نفس القيد.

---

## ترتيب التنفيذ المقترح

**٠.** قراءة دليل الهوية ومطابقة التوكنات الأساسية بيه.
**١.** المرحلة 1 — قراءة مكسورة فعليًا (التوكنات الناقصة، القرمزي كنص، البانرات الفاتحة).
**٢.** المرحلة 1.5 — التنسيقات المتداخلة (الهيدر + مسح `ms`/`me`).
**٣.** المرحلة 4.1 و4.1.b و4.2 — مكاسب سريعة ومخاطرة شبه صفر.
**٤.** المرحلة 3 — تقليل التكرار (بيسهّل كل اللي بعده).
**٥.** المرحلة 2 — سلّم الطباعة والمسافات.
**٦.** المرحلة 4 الباقي (الخطوط، `td.p-4`).
**٧.** المرحلة 4.5 — الثيم الفاتح (أكبر بند، ومتوقف على 1 و2 و3).
**٨.** المرحلة 5 — تحقق نهائي على الثيمين.

كل مرحلة في commit منفصل، وميتبدأش اللي بعدها غير بعد تحقق أخضر كامل.

## قرارات مطلوبة قبل البدء
1. ~~**دليل الهوية** — ينسخ جوه مجلد المشروع عشان يتقرا (بند "المرجع").~~ **✅ اتقرا.**
2. ~~**بند 1.1** — نعرّف التوكنين الناقصين بقيم جديدة، ولا نستبدلهم بـ `--color-foreground` و`--color-text-secondary`؟~~ **✅ اتحل (بند 1.1 أعلاه).**
3. ~~**المرحلة 4.5** — الثيم الافتراضي: فاتح، غامق، ولا يتبع النظام؟~~ **✅ قرار: يفضل غامق افتراضيًا حتى بعد إضافة الفاتح.**

### ✅ دليل الهوية — النتيجة
اتقرا دليل الهوية كامل. خلاصة اللي يخص الكود:

**الألوان (قاعدة 70/20/10):**
- Charcoal Black `#1C1C1C` (70%) — قاعدة الخلفية.
- Soft White `#F0F0F0` (20%) — لون النص.
- Deep Crimson `#941414` (10%) — تمييز/accent.

**تأكيد مهم:** الثيم الغامق الحالي في `globals.css` مطابق للدليل **بالظبط** — `--color-background: #1C1C1C`، `--color-foreground: #F0F0F0`، `--color-primary: #941414` — من غير ما نكون قرينا الدليل وقتها. يعني **مفيش أي تعديل مطلوب على ألوان الثيم الغامق**، ده كان صح من الأول.

**الطباعة:**
- إنجليزي: `Playfair Display` للعناوين (مطابق للمستخدم فعلًا في `font-display`/شعار "d")، و`Archivo` للنص الأساسي (كان اتشال في بند 4.4 باعتباره احتياطي مش مستخدم — طلع مطلوب رسميًا).
- عربي: `Tharwat Emara Ruqaa` للعناوين و`Bahij TheSansArabic` للنص — خطوط تجارية من Bahij، مش متاحة مجانًا على Google Fonts.

**✅ تم التنفيذ (خط Archivo):** رجّعت `Archivo` عبر `next/font/google` في `lib/fonts.ts` (400/500/700)، وضفته لـ `className` في `app/layout.tsx` و`global-error.tsx`. `globals.css` بقى بيفرّق بـ `html[lang="en"] body { font-family: var(--font-archivo)... }` — العربي فاضل `Tajawal` زي ما هو من غير أي تغيير. شلت كمان تجاوزين صريحين لـ `--font-tajawal` كانوا هيكسروا القاعدة الجديدة في هيدر الداشبورد (تاريخ اليوم + مربع البحث) عشان يورثوا الخط الصح حسب اللغة تلقائيًا.

**قرار المستخدم بخصوص الخط العربي:** نفضل على `Tajawal` دلوقتي (مش `Bahij TheSansArabic`) لحد ما يوصلنا ملفات الخط نفسها من المستخدم — وقتها نستبدلها.

**✅ تم التنفيذ.** شلت كل الـ6 مواضع (طلعت فعليًا في إعدادات `XAxis`/`YAxis`/`Tooltip` عبر الـAreaChart والـPieChart) بدل ما أستبدلها بـ`--font-archivo`/`--font-tajawal` صريح — لأن بعد بند "خط Archivo" فوق، القاعدة العامة `html[lang="en"] body` بقت بتحدد الخط الصح تلقائيًا حسب اللغة، فكتابة الخط يدويًا جوه Recharts كانت هتتعارض معاها (وهتفضل Tajawal حتى في الإنجليزي). الشيلان يخلّي عناصر الرسم البياني ترث الخط الصح من `body` زي أي نص تاني في الصفحة. تحقّقت بالـgrep إن مفيش أي `font-tajawal` متبقي في الملف.
